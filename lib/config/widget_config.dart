/// WidgetConfig saves the widget logic
/// and manages them. For Example :
///  - How many slider widget should be loaded at first,
///    using by [Lazy Loader]
class WidgetConfig {
  static final Map<String, int> sliderRange = {
    'start': 0,
    'end': 3,
  };
}
