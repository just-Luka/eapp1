/// All 'shared_preferences' keywords
enum SPKeyword {
  // App keywords
  locale,
  theme,
  isFirstBootstrap, // means, that there is no caches, yet!

  // Firestore keywords
  category,
  hotel,
  slider,
}
