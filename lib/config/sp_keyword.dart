/// All 'shared_preferences' keywords
enum SPKeyword {
  // App keywords
  locale,
  theme,
  isFirstBootstrap, // means, that there are not caches, yet!

  // Firestore keywords
  category,
  hotel,
  slider,
  bookmark,
}
